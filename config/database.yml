# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlserver
  dsn: local_sqlserver
  mode: odbc
  database: test
  username: <%= Rails.application.secrets.DOMAIN_USER %>
  password: <%= Rails.application.secrets.LDAP_SECRET %>

development:
  adapter: sqlserver
  dsn: local_sqlserver
  mode: odbc
  database: test
  username: <%= Rails.application.secrets.DOMAIN_USER %>
  password: <%= Rails.application.secrets.LDAP_SECRET %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: sqlserver
  dsn: local_sqlserver
  mode: odbc
  database: test
  username: <%= Rails.application.secrets.DOMAIN_USER %>
  password: <%= Rails.application.secrets.LDAP_SECRET %>

production:
  adapter: sqlserver
  dsn: local_sqlserver
  mode: odbc
  database: test
  username: <%= Rails.application.secrets.DOMAIN_USER %>
  password: <%= Rails.application.secrets.LDAP_SECRET %>
